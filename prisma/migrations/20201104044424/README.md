# Migration `20201104044424`

This migration has been generated by Joel Gustafson at 11/3/2020, 11:44:24 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."schemas" ADD COLUMN "draftContent" text   NOT NULL DEFAULT E'',
ADD COLUMN "draftReadme" text   

CREATE TABLE "public"."_Import" (
"A" text   NOT NULL ,
"B" text   NOT NULL 
)

CREATE UNIQUE INDEX "_Import_AB_unique" ON "public"."_Import"("A", "B")

CREATE INDEX "_Import_B_index" ON "public"."_Import"("B")

ALTER TABLE "public"."_Import" ADD FOREIGN KEY ("A")REFERENCES "public"."schema_versions"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."_Import" ADD FOREIGN KEY ("B")REFERENCES "public"."schema_versions"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201103001531-agent-unique-links..20201104044424
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
     provider = "postgresql"
-    url = "***"
+    url = "***"
 }
 model Account {
     id                 String    @id @default(uuid())
@@ -116,8 +116,10 @@
     agent         Agent                @relation(fields: [agentId], references: [id])
     createdAt     DateTime             @default(now()) @map(name: "created_at")
     updatedAt     DateTime             @default(now()) @updatedAt @map(name: "updated_at")
     isPublic      Boolean              @map(name: "is_public")
+    draftContent  String               @default("")
+    draftReadme   String?
     versions      SchemaVersion[]
     collaborators SchemaCollaborator[]
     @@unique([agentId, slug])
@@ -132,11 +134,13 @@
     agentId         String              @map(name: "agent_id")
     agent           Agent               @relation(fields: [agentId], references: [id])
     schemaId        String              @map(name: "schema_id")
     schema          Schema              @relation(fields: [schemaId], references: [id])
+    imports         SchemaVersion[]     @relation("Import")
+    importedBy      SchemaVersion[]     @relation("Import")
+    createdAt       DateTime            @default(now()) @map(name: "created_at")
     dataCollections CollectionVersion[] @relation("DataSchema")
     metaCollections CollectionVersion[] @relation("MetaSchema")
-    createdAt       DateTime            @default(now()) @map(name: "created_at")
     @@map(name: "schema_versions")
 }
```


